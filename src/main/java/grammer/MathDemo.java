package grammer;

public class MathDemo {
    public static void main(String[] args) {
        int tmp = (1 << 2) - 1; // 移位优先级小于加减运算
        int a = (int)Math.pow(2, 3); // double pow(double, double) {}
    }
}
